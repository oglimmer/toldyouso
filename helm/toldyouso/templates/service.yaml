apiVersion: v1
kind: Service
metadata:
  name: {{ include "toldyouso.fullname" . }}
  labels:
    {{- include "toldyouso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "toldyouso.selectorLabels" . | nindent 4 }}
